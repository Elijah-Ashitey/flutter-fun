import 'package:flutter/material.dart';

class UIModel {
  String title;
  String description;
  String image;
  Widget screen;
  UIModel({
    required this.title,
    required this.description,
    required this.image,
    required this.screen,
  });
}
